import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'shere-a-read',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
